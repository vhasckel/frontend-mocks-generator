# API keys e segredos ficam só em variáveis de ambiente — nunca no código (SPEC §12).
# Obtenha uma chave gratuita em https://aistudio.google.com/apikey
GOOGLE_API_KEY=
# Opcional — default: gemini-2.0-flash (faixa gratuita do Gemini Developer API)
GEMINI_MODEL=gemini-2.0-flash
PROJECT_ROOT=.
MOCKS_OUTPUT_DIR=examples/mocks
MAX_FILE_SIZE_BYTES=100000
